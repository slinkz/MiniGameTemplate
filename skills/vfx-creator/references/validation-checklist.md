# VFX 验证清单（短版）

## 一、先看接线
- [ ] `SpriteSheetVFXSystem` 已接到当前场景对象
- [ ] 目标 `VFXTypeSO` 已接到触发脚本或 Demo 脚本
- [ ] `VFXTypeSO` 已存在于 `VFXTypeRegistrySO`
- [ ] 当前场景不是 `Boot`

## 二、再看类型选择
- [ ] 当前输入/触发逻辑确实选中了目标类型
- [ ] 没有被默认值、旧序列化值或 `RuntimeIndex` 覆盖

## 三、再看运行时映射
- [ ] Registry 中的顺序与运行时索引一致
- [ ] 材质、图集、帧数、循环参数正确
- [ ] 非循环特效能自动回收

## 四、最后看视觉表现
- [ ] 验证样本肉眼足够可区分
- [ ] Layer / Blend / Tint 没有把差异吃掉
- [ ] 位置、缩放、朝向在场景里可见

## 五、业务链路验证
- [ ] `VFXDemo` 中可直接播放
- [ ] 若新增了一个 Demo 类型，README 与场景接线说明已同步更新
- [ ] 若本轮改动涉及 `[SerializeField]` 默认值或新增字段，已显式检查并更新对应 `.unity` / `.prefab` 实例序列化数据
- [ ] 若接入业务系统，真实链路能触发
- [ ] 当前 Demo 的主验收路径已贴合真实场景，而不是假想路径


## 六、收尾
- [ ] 更新任务文件
- [ ] 更新 CHANGELOG / README（如需要）
- [ ] 跑静态检查
- [ ] 跑 Unity batchmode 冷启动编译
