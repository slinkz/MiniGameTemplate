# FairyGUI_Export 目录

由 FairyGUI 编辑器发布输出的资源（bytes + 纹理），**自动生成，不要手动修改**。

## 来源
- 由 `UIProject/` 中的 FairyGUI 编辑器工程发布生成
- 发布路径配置在 `UIProject/settings/Publish.json`

## 注意
- 此目录内容由 FairyGUI 编辑器管理
- 重新发布会覆盖此目录中的文件
- 已纳入 Git 版本控制（Unity 需要这些资源）
